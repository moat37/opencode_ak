Executes a given bash command in a persistent shell session, ensuring proper handling and security measures.

Before executing the command, please follow these steps:

# Command Execution:
   - Always quote file paths that contain spaces with double quotes (e.g., cd "path with spaces/file.txt")
   - Examples of proper quoting:
     - python "/path/with spaces/script.py" (correct)
     - python /path/with spaces/script.py (incorrect - will fail)
   - After ensuring proper quoting, execute the command.
   - Capture the output of the command.

Usage notes:
  - The command argument is required.
  - When issuing multiple commands, use the ';' or '&&' operator to separate them. DO NOT use newlines (newlines are ok in quoted strings).
  - Use search commands like `find`, `grep`, and `ls` proactively.
  - NEVER use `cd`. Always use absolute paths.
    <good-example>
    pytest /foo/bar/tests
    </good-example>
    <bad-example>
    cd /foo/bar && pytest tests
    </bad-example>

# How to call 00_tools/annual_report_reader.py
  Run the following bash command —
  $ python3 00_tools/annual_report_reader.py "<questions>" <annual_report_file_path>
  Examples of the tool call:
  $ python3 00_tools/annual_report_reader.py "What are the main objectives that the management is driving the company towards? Has the management stated a vision for the company? Are there any stated milestones?" TCS/annual_reports/annual_report_2024.txt
  $ python3 00_tools/annual_report_reader.py "Is this company a part of the TATA Group? A company is deemed to be a part of a group if it explicitly states so. Else, it is considered to not be a part of that group." TITAN/annual_reports/annual_report_2024.txt

# How to call 00_tools/multi_file_reader.py
  Run the following bash command —
  $ python3 00_tools/multi_file_reader.py "<questions>" <file_path1> <file_path2> ...
  Examples of the tool call:
  $ python3 00_tools/multi_file_reader.py "What are the main objectives that the management is driving the company towards? Has the management stated a vision for the company? Are there any stated milestones?" TCS/concalls/concall_transcript_2023_Jan.txt TCS/concalls/concall_transcript_2023_Apr.txt TCS/concalls/concall_transcript_2023_Jul.txt
  $ python3 00_tools/multi_file_reader.py "What was the capacity utilization in the last two quarters? What is the status of the capex projects?" MAITHANALL/concalls/concall_transcript_2025_Jul.txt MAITHANALL/concalls/concall_transcript_2025_Apr.txt

